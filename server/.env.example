# Copy thành .env rồi điền giá trị thật:
#   cp .env.example .env

PORT=3030
# DATABASE_URL=postgres://user:pass@127.0.0.1:5432/dbname

# Wealify LLM — bắt buộc nếu bật llmEnabled trên admin
WEALIFY_LLM_API_KEY=
WEALIFY_LLM_BASE_URL=https://llm.wealify.app/v1
